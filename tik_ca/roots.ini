
[ovpn]
private = file://ovpn-ca-key.pem
certificate = ovpn-ca.pem
config = config.json
dbconfig = db-config.json

[capsman]
private = file://capsman-ca-key.pem
certificate = capsman-ca.pem
config = config.json
dbconfig = db-config.json

[cli]
private = file://cli-ca-key.pem
certificate = cli-ca.pem
config = config.json
dbconfig = db-config.json
