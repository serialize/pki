
[etcdserver]
private = file://etcd-server-key.pem
certificate = etcd-server.pem
config = config/config-etcd-server.json
dbconfig = db-config.json

[etcdpeer]
private = file://etcd-peer-key.pem
certificate = etcd-peer.pem
config = config/config-etcd-peer.json
dbconfig = db-config.json

[etcdclient]
private = file://etcd-client-key.pem
certificate = etcd-client.pem
config = config/config-etcd-client.json
dbconfig = db-config.json

[docker]
private = file://docker-key.pem
certificate = docker.pem
config = config/config.json
dbconfig = db-config.json
